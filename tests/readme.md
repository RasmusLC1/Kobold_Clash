# Kobold Clash - Test Suite

This directory contains the automated test suite for *Kobold Clash*. The suite uses `pytest` to validate core engine algorithms, state logic, and game initializations without requiring a full graphical display window to open.

## Prerequisites

Before running the tests, ensure you have `pytest` installed in your Python environment:

### bash
pip install pytest

## To run tests
py -m pytest

